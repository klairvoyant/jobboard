<%= simple_form_for(@interest_area) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :interest %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
